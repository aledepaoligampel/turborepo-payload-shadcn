import { Media } from './collections/Media'
import { Users } from './collections/Users'

export const collections = [Users, Media]

// También exportamos individualmente por si se necesitan
export { Users, Media }
